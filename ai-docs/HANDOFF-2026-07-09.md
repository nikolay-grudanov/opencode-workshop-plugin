# Handoff snapshot — opencode-workshop-plugin fork, paused 2026-07-09

> Этот файл — резервная копия handoff-снапшота.  
> Основной снапшот сохранён в Hindsight (см. `mcp_hindsight_recall(query="handoff opencode-workshop-plugin 2026-07-09")`).  
> Если Hindsight не подгружается в новой сессии — читай этот файл.

## Текущее состояние (где остановились)

**Repo (live on GitHub):** https://github.com/nikolay-grudanov/opencode-workshop-plugin  
**Branch:** main  
**HEAD:** `7576ff0` (handoff doc added)  
**Current version:** `@grudanov-nikolay/opencode-workshop-plugin@0.1.0-kolya.6`  
**Local clone:** `/home/gna/workspase/projects/opencode-workshop-plugin/` (moved from `/tmp/oswp-clone` 2026-07-10 — `/tmp` gets wiped on reboot, this would have lost the work)
- **Workshop fork (separate repo):** `/home/gna/workspase/projects/opencode-workshop/` (moved from `/home/gna/project/opencode-workshop` on 2026-07-10)

## Git history (9 коммитов, всё запушено)

```
9c6f7fb refactor(F-002.1): prettier reformat + section markers in dist/
bf0e4ac feat(F-002.5): GitHub Action smoke + scripted A/B test
d598c9a feat(F-002.6): trace_only mode routes logs to ~/.raindrop/trace.log
d2c2288 docs: rewrite README to upstream-style (was auto-generated by gh)
e67f7c2 feat(F-002.4): install-local.sh for OpenCode plugin cache
463f205 docs(F-002): plan dev-experience sub-features
1e9b275 fix(F-001): smoke test PASSES, bump to v0.1.0-kolya.3
f545c0e fix(F-001): handle MCP tool results missing .output field
8c784fd chore: initial repo from upstream @raindrop-ai/opencode-plugin@0.0.18
```

## Что сделано

### F-001 — Fix `tool.execute.after: result.output is required` для MCP tools
- Падает на каждом MCP-туле (GigaChat 3 Ultra через MLProxy, jupyter_executor)
- Upstream issue: anomalyco/opencode#21149 (PR #21150 закрыт 2026-05-15)
- Файл: `dist/index.js` + `dist/index.cjs`, grep `KOLYA PATCH (F-001)`
- Суть: при `result.output === undefined` и `Array.isArray(result.content)` — собрать текст из `content[].text`, joined `\n`
- Verified: A/B smoke test (original: 2x errors; наш: 0 errors, status=OK, 5 spans)

### F-002 — Developer experience (closed, 4 sub-features)

- **F-002.4 install-local.sh:** 1 команда (`./scripts/install-local.sh`) вместо 15 мин возни с `~/.cache/opencode/packages/`. Idempotent: install/uninstall/reinstall/--help
- **F-002.5 GitHub Action:** `.github/workflows/smoke.yml` — syntax-check на каждый push (ловля F-001 regression), smoke-test на workflow_dispatch (полный e2e). `scripts/smoke-test.sh` — A/B test scripted (~180 lines)
- **F-002.6 trace_only:** `raindrop.json` поле `trace_only: true` или env `RAINDROP_TRACE_ONLY=true` → `console.log/warn/error` → `~/.raindrop/trace.log` (append с ISO timestamp). TUI stdout остаётся чистым. Backwards-compatible default false.
- **F-002.1 prettier reformat:** `bunx prettier --parser meriyah --print-width 200` оба бандла. ESM dist/index.js 2290→2122 lines, CJS 2306→2133 lines. 8 SECTION markers (`// === SECTION: hook: <name> ===`) — grep-friendly

## Что осталось в Backlog (USER-FACING, не dev-experience)

Из `ai-docs/PLAN.md`:
- **F-003** — Reverse-engineer full `src/index.ts` (можно пропустить, F-002.1 уже даёт читаемый dist)
- **F-004** — Harden `RAINDROP_EVENT_METADATA` (JSON validation + warning)
- **F-005** — `RAINDROP_LOCAL_WORKSHOP_URL` env precedence fix — **REAL BUG**, Коля ловил 2026-06-30
- **F-006** — Multi-agent trace correlation
- **F-007** — Extended test coverage
- **F-008** — npm publish automation via GitHub Actions
- **F-009** — Update package.json URLs once npm live

## Ключевые правила (НЕ забывать)

1. **Miko-no-auto-commit**: никаких `git push` без явного «push» от Коли
2. **NO SELF-RESTART**: не рестартить Workshop, OpenCode, gateway без явного OK
3. **Best is enemy of good enough**: Коля отвергает overengineering
4. **PLAN.md single source of truth**: новые work items как `### F-NNN` секции с `- [ ]` чекбоксами, каждый коммит обновляет PLAN.md
5. **KOLYA PATCH (F-NNN)** convention для всех патчей в dist/

## Известные подводные камни

### OpenCode 1.17.x plugin loading
- ❌ `npm link` НЕ работает
- ❌ `file:` в package.json НЕ работает
- ✅ Только `scripts/install-local.sh` или manual edit global `~/.config/opencode/opencode.json`

### CJS vs ESM
- В CJS `localWorkshopUrl: resolveLocalDebuggerBaseUrl(...)` (до prettier reformat)
- После F-002.1 prettier оба используют `resolveLocalWorkshopUrl` (потому что reformat работает с upstream-чистым кодом)
- Всегда grep оба файла перед manual patches

### Plugin metadata hardcoded v0.0.18
- В Workshop всегда показывается `plugin_version: 0.0.18` и `library.name: @raindrop-ai/opencode-plugin` — hardcoded в dist

### OpenCode 1.17.x debug logs
- `RAINDROP_DEBUG=true` НЕ приводит к plugin logs в stdout
- Smoke-test полагается на grep F-001 throw + Workshop API, не на stdout

## Что я НЕ делала (нужен явный OK)

- ❌ `npm publish` в `@grudanov-nikolay` scope (требует `npm login`)
- ❌ `git tag` (например `v0.1.0-kolya.6`)
- ❌ `gh release create` (interactive)
- ❌ PR upstream с F-001 fix (нет публичного src)

## Когда Коля вернётся — разумно спросить

1. Нужен ли `npm publish`? (paid npmjs account?)
2. `gh release create v0.1.0-kolya.6`?
3. PR upstream с F-001 fix?
4. Приоритизировать F-005 (env precedence fix)?

## Контекст Коли который помню

- Специальность 2.3.1/5.2.2 — VKR/dissertation "Methods and algorithms for generation and validation of synthetic urban mobility trajectories"
- Supervisor: Смирнов Д.С.
- Встреча со Смирновым: PDF с разбором 35 пунктов, саммари, речь, 15 диаграмм
- "best is enemy of good enough" — ключевой принцип
- Locked-in: mcp_fetch как extract_backend (НЕ менять), camofox на tmux (НЕ мигрировать на PM2)
- Miko Destructive-Op Safety Rule (priority 100, system-prompt-injected, 2026-06-14)
- Если Kolya вернётся с просьбой удалить что-то — ОБЯЗАТЕЛЬНО `hindsight_recall` сначала, не полагаться на stale memory

## Версия окружения

- Node.js: v26.2.0
- Bun: 1.3.14
- OpenCode: 1.17.11
- @opencode-ai/plugin: >=1.3.0
- Workshop daemon: localhost:5899 (pid 2074 на момент F-001 test)
- npm upstream tarball: 0.0.18, 41KB
- Workshop repo fork: `~/project/opencode-workshop/` (ОТДЕЛЬНЫЙ от плагина!) — branch main at `914d74d` (release v0.1.15)

---

**When:** 2026-07-09 22:55 MSK  
**Status:** PAUSED, ready for handoff  
**Next action:** when Kolya returns, run `mcp_hindsight_recall(query="handoff opencode-workshop-plugin 2026-07-09", budget="mid")` — see this doc.
